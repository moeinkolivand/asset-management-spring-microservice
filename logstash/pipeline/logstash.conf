input {
    beats {
        port => 5044
    }
}

filter {
    json {
        source => "message"
        target => "@metadata"
    }
}

output {
    elasticsearch {
        hosts => ["http://elasticsearch:9200"]
        index => "application-logs-%{+YYYY.MM.dd}"
    }

    stdout {
        codec => rubydebug
    }
}